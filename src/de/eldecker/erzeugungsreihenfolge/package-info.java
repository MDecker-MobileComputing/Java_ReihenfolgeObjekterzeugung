/**
 * Alle Unterpaket enthalte eine Java-Datei mit Namen {@code Beispiel} gefolgt 
 * von der Nummer. Dies war erforderlich, damit Klassennamen wie {@code S1} oder 
 * {@code O1} in mehreren Beispielen verwendet werden können.
 * <br><br>
 * 
 * Die Ausgabe für die einzelnen Beispiel ist immer in der jeweiligen {@code package-info.java}
 * enthalten (damit nicht gleich beim Betrachten des Quellcodes die Lösung verraten wird).
 */
package de.eldecker.erzeugungsreihenfolge;